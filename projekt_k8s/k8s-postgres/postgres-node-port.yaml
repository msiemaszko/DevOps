apiVersion: v1
kind: Service
metadata:
  name: postgres-node-port

spec:
  type: NodePort
  ports:
    - port: 5432
      targetPort: 5432
      nodePort: 31001
  
  selector:
    app: comp-app
    component: db