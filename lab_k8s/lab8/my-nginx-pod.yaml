apiVersion: v1
kind: Pod
metadata:
  name: my-nginx-pod
  labels:
    component: web
    app: my-app

spec: 
  containers:
    - name: my-nginx
      # image: nginx:alpine
      image: marekprezes/mynginxk8s
      ports:
        - containerPort: 80 
